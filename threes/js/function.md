JS 函数的 “反直觉” 本质
Java / Go 的函数是「“死” 的执行逻辑」—— 固定依附于类 / 包、类型严格、作用域清晰、调用方式固定；JS 的函数是「“活” 的可执行实体」—— 可赋值、可传递、可嵌套、作用域灵活、this 动态。
对你来说，最关键的转变是：不要把 JS 函数当成 Java / Go 的 “方法”，而是当成 “可以执行的变量”，很多用法（回调、闭包、函数嵌套）就顺理成章了。